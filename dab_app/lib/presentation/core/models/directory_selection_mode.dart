/// [ARCH: PRESENTATION_CORE]
/// ROLE: How Directory rows pick people.
enum DirectorySelectionMode {
  /// One person at a time (Reports team-read).
  single,

  /// Any subset of people or groups (Explorer / Insights filters).
  multi,
}
