import 'package:flutter_test/flutter_test.dart';

/// Default Flutter template test removed — screen logic is covered under
/// `test/presentation/` with Riverpod + mocks.
void main() {
  test('dab_app test harness loads', () {
    expect(true, isTrue);
  });
}
