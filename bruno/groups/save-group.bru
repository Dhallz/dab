meta {
  name: Save Group
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{targetGroupName}}",
    "memberIds": []
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
