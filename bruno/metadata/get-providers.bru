meta {
  name: Get Provider Metadata
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/metadata/providers
  body: none
  auth: inherit
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
