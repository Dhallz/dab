meta {
  name: Sync Phorge Users
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/users/sync
  body: none
  auth: inherit
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
