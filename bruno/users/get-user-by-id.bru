meta {
  name: Get User By Id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/users/{{targetUserId}}
  body: none
  auth: inherit
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
